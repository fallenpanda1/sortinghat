# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

# Can't do much if our third party dependencies have warnings, so ignore them
inhibit_all_warnings!

target 'Sortinghat' do
  use_frameworks!

  # Pods for 'Sortinghat'
  pod 'RxSwift',  '~> 4.0'
  pod 'RxCocoa',  '~> 4.0'
  pod 'RxAnimated'
end
